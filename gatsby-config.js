module.exports = {
  siteMetadata: {
    title: `landing`,
    siteUrl: `https://www.yourdomain.tld`,
  },
  plugins: [
     `gatsby-plugin-image`,
     `gatsby-plugin-sharp`,
      `gatsby-source-filesystem`,
      `gatsby-transformer-sharp`,
  ],
}
